Given an input string s, reverse the order of the words.

A word is defined as a sequence of non-space characters. The words in s will be separated by at least one space.

Return a string of the words in reverse order concatenated by a single space.

Note that s may contain leading or trailing spaces or multiple spaces between two words. The returned string should only have a single space separating the words. Do not include any extra spaces.

 

Example 1:

Input: s = "the sky is blue"
Output: "blue is sky the"
Example 2:

Input: s = "  hello world  "
Output: "world hello"
Explanation: Your reversed string should not contain leading or trailing spaces.
Example 3:

Input: s = "a good   example"
Output: "example good a"
Explanation: You need to reduce multiple spaces between two words to a single space in the reversed string.
 

Constraints:

1 <= s.length <= 104
s contains English letters (upper-case and lower-case), digits, and spaces ' '.
There is at least one word in s.

Approach - Reversing the whole string first and then reversing each word
// TC = O(N) , SC = O(1)

class Solution {
public:
    string reverseWords(string s) {
        long long n = s.size(); // n is initialized with the size of string 
        reverse(s.begin(), s.end()); // reversing the whole string "the pen" ---> "nep eht"

        string ans; // resultant string

        int i; // initialising i here bcoz we are going to use same i in both outer loop and inner loop
        for(i = 0; i < n; i++) // total there are two lops but as we are using same i and incrementing i in both loops it acts as the same loop and TC becomes O(N)
        {
            string word = ""; // string for each word reversal
            while(i < n && s[i] != ' ') // till we dont hit a space traverse , i.e taking a word here
            {
                word += s[i]; // each word is added in string 
                i++; // incrementing i
            }

            reverse(word.begin(), word.end()); // reversing the whole word "nep" --> "pen"
            if(word.size() > 0) // we are going to add only valid words , this if condiition also removes the (extra spaces as extra spaced -> so called word form) as extra space is not counted and its size is 0 only
            {
                ans += " " + word; // add each word with a " " + word to make it so that each word is separated by exactly one space only
            }
        }

        return ans.substr(1); // as each word is added with a  " " - space before it, so we need to return the substr of ans with substring starting from 1 so that " pen the" becomes "pen the"
    }
};

Follow-up: If the string data type is mutable in your language, can you solve it in-place with O(1) extra space?
