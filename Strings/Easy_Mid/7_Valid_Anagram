LC - 242

Valid Anagram

Given two strings s and t, return true if t is an anagram of s, and false otherwise.

 

Example 1:

Input: s = "anagram", t = "nagaram"

Output: true

Example 2:

Input: s = "rat", t = "car"

Output: false


class Solution {
public:
    bool isAnagram(string s, string t) {
        /*
        Brute Force
        // TC - O(nlogn)
        // SC - O(1)
        if(s.size() != t.size()) // if s.size != t.size , we cant never get anagrams, so directly return false
        {
            return false;
        }
        sort(s.begin(), s.end()); // sort both the strings // O(nlogn)
        sort(t.begin(), t.end()); // O(nlogn)

        if(s == t) // now check if s == t, then return true
        {
            return true;
        }

        return false; // else return false
        */

        /*
        Optimal Solution
        TC - O(n)
        SC - O(1)
        if(s.size() != t.size()) // if s.size != t.size , we cant never get anagrams, so directly return false
        {
            return false;
        }


        vector<int> sv(26, 0); // sc - O(1) // we are going to store frequencies of both the strings s and t, so we are taking two vectors of size 26 to store a-z, if it was more characters like A-Z then use vector size of 256
        vector<int> tv(26, 0); // sc - O(1)

        for(int i = 0; i < s.size(); i++) // 0(n) // now store the freq of each character for s string
        {
            sv[s[i] - 'a']++;
        }

        for(int i = 0; i < t.size(); i++) // O(n) // now store the freq of each charcter of t string
        {
            tv[t[i]- 'a']++;
        }        

        for(int i = 0; i < 26; i++) // O(26) -> O(1) // now check if each character has same frequency in both the vectors sv and tv
        {
            if(sv[i] != tv[i]) // if any charcter freq mismatches in both the vectos directly return false
            {
                return false;
            }
        }

        return true; // if every character freq matches in both the vectors , then return true
        
    */
    }
};
